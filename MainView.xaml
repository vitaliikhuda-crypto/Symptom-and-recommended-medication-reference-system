<Window x:Class="MedSearchApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MedSearch" Height="500" Width="800">

    <DockPanel>

        <!-- Меню -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Background="LightGray">
            <Button Content="Головна" Width="100" Margin="5" Click="ShowHome"/>
            <Button Content="Результати" Width="120" Margin="5" Click="ShowResults"/>
            <Button Content="Інфо" Width="100" Margin="5" Click="ShowInfo"/>
        </StackPanel>

        <!-- Frame -->
        <Frame x:Name="MainFrame"/>

    </DockPanel>
</Window>
